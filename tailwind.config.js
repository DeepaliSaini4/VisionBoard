/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx,html}",  // Adjust based on your project structure
  ],
  theme: {
    extend: {
      // You can extend the default theme here (colors, spacing, fonts, etc.)
    },
  },
  plugins: [
    // Add Tailwind plugins here if needed, e.g., forms, typography
  ],
};
